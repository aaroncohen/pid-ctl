# pid-ctl

Composable PID controller for the command line and as a Rust library: deterministic control math, state snapshots, and Unix-friendly I/O.

Design, behavior, and CLI contracts are specified in [`pid-ctl_plan.md`](./pid-ctl_plan.md).
